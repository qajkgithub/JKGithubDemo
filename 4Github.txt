This is my Git test for gitDemoDev branch

Set Up Git:
git config --global user.name "qajkgithub"
git config --global user.email "youremail@domain.com"

other commands
git clone (url)

git add (file names)   - Add a new file locally
git commit -m "(committed message) - Commit the added file to the branch locally
git push -u origin master - Updated the change of local branch to the
centrolized GitHub

git remote add origin master (url)